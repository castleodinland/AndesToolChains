on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: castleodinland/build_docker_and_run@master 
      
      - name: Run a nds32 command
        run: nds32le-elf-gcc
